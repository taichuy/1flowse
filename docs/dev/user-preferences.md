# User Preferences (Deprecated)

This file is no longer the shared source of truth.

- Shared repo conventions now live in `docs/dev/team-conventions.md`.
- Per-developer local notes now belong in `docs/.private/` and must stay gitignored.
- Historical single-maintainer notes were archived to `docs/expired/2026-03-17-single-maintainer-user-preferences-expired.md`.

Do not add new shared rules here. Promote them to `AGENTS.md`, `docs/dev/team-conventions.md`, `.agents/skills/*`, or `docs/adr/` depending on scope.
