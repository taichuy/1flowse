# 文档计划审计待讨论

更新时间：`2026-04-15 00`

说明：本文件承接 `docs/qa-report/document-plan-audit.md`，继续围绕同主题滚动更新，不新建同主题新文件。

## 当前已确认事实

- 当前工程推进速度很快，近 24 小时已经完成一轮明显的后台治理和设置区增强。
- 当前关键验证是绿的：
  - `web/app` 目标回归 `25` 个用例通过
  - `api-server openapi_docs_tests` `6` 个用例通过
  - `style-boundary page.settings` 提权后通过
- 当前主问题已经不是“代码质量差”或“工程跑不起来”。
- 当前更核心的问题是：
  - 主产品闭环没有跟上治理速度
  - `03 Flow 前置容器` 仍停留在文档主线，没有进入实际代码主线

## 当前最值得拍板的问题

1. 是否把 `03 Flow 前置容器` 设为当前唯一产品主线。
   - 建议：`是`
2. 是否冻结新增 `settings / docs / role / shell` 专题，只允许阻塞修复。
   - 建议：`是`
3. 是否用“每天至少一个用户可见闭环增量”来替代旧式进度判断。
   - 建议：`是`

## 如果按建议推进，下一批最值得做的内容

- 应用列表页
- 创建应用
- 应用概览页
- 概览页进入 Flow 主入口

## 不建议继续优先做的内容

- 再开一轮设置页新专题
- 再扩内部 API 文档的展示花样
- 在 `plugin-runner`、`publish-gateway` 还只是骨架时继续提前做过深治理

## 本轮结论摘要

- 当前项目不是慢，而是快。
- 当前问题不是工程坏，而是价值闭环排序还没切过来。
- 大方向不用改，执行焦点要改。
