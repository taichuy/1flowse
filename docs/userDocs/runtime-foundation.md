# 运行历史摘要

- 2026-04-10 11:56-18:57 CST：P1 主线已收敛为工作流、运行时、发布优先；`01-07` 已完成核心边界确认。当前基线含：应用级 `root` + 空间级角色、权限点并集鉴权、工作台最小概览、`agentFlow` 命名、仅 `Publish` 生效、运行时状态机主表 + 事件日志、发布网关控制面入口 + 薄代理、状态与记忆显式读写 + 快照注入。
- 2026-04-10 19:32 CST：协作偏好新增。实现时偏向先整体铺开再按验证修复；讨论建议需同时写清现象/结果、收益/风险和明确建议。
- 2026-04-10 19:36 CST：收尾偏好新增。每次结束自动提交本次相关文件；若有无关改动，需隔离提交范围。
- 2026-04-10 20:03 CST：`08 插件体系` 已定稿。确认双轨插件模型、四类插件、统一包结构、来源分级、共享 `plugin-runner`、本机 RPC、状态机生命周期、升级回滚和“禁止插件间硬依赖”。
- 2026-04-10 20:55 CST：`08` 已进入实施计划阶段。实现顺序拆为：插件后端基础 -> 控制面 API -> 运行时集成；首份计划为 `docs/superpowers/plans/2026-04-10-plugin-framework-foundation.md`。
- 2026-04-10 21:13 CST：技术选型讨论新增部署基线决策。P1 部署目标先定为 `单机/单区域 + Docker Compose`；架构继续保持控制面、运行时、插件 runner 可拆边界，但 P1 不提前为 `K8s` 或多区域付出额外复杂度。
- 2026-04-10 21:15 CST：技术选型讨论新增前端范围决策。P1 前端形态先定为 `控制台优先`，聚焦登录后后台、工作台、`agentFlow` 编辑器、调试页和发布文档页；官网、营销页和公开站不进入当前主线。
- 2026-04-10 21:16 CST：技术选型讨论新增前端交付边界。`Publish/API` 文档页不需要匿名公开；展示组织参考 `../dify`，但仍属于登录后控制台能力。前端主战场聚焦 `chatflow/agentFlow` 编辑与调试，不额外扩展公开站形态。
- 2026-04-10 21:24 CST：技术选型讨论新增编辑协同边界。P1 不支持多人实时协同编辑，先采用单人草稿编辑 + 显式 `Publish` 生效模型；线上与外部交付均以发布版本为准。
- 2026-04-10 21:34-21:45 CST：技术栈关键项已收敛。前后端通信采用 `REST + SSE`，控制台鉴权采用参考 `Dify` 的 `Cookie Token(access/refresh/csrf)`，对象存储沿用 Docker 内 `RustFS`，仓库采用 `单仓 Monorepo`。前端最终选 `React + Vite + TanStack Router + Ant Design + CSS Modules/CSS Variables + TanStack Query + Zustand + xyflow + Lexical + Monaco`；不使用 `Tailwind`，交互参考 `Dify`，但不沿用其 `Next.js + Tailwind + 自建 base 组件` 路线。

# 下一步计划

- 先基于 `单机/单区域 + Docker Compose` 基线收敛 P1 技术栈、仓库组织、进程拓扑、前后端通信、存储与观测方案。
- 基于“控制台优先 + 非匿名文档 + 单人编辑发布模型”，下一步一次性确认剩余技术约束：前端渲染策略、前后端通信模型、存储组件和观测基线。
- 下一步进入技术选型设计稿，继续收敛后端框架、进程拓扑、数据分层、接口契约和部署结构。
- 技术选型整体确认后，再写入产品架构设计文档，并反推 Rust workspace、前端 Monorepo 与部署清单。
- 插件实现仍按既定顺序推进：基础能力 -> 控制面 API -> 运行时集成。
